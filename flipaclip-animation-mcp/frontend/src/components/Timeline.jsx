export function Timeline({frames,current,onChange,onAdd,onDuplicate}){return <div className="timeline"><button onClick={onAdd}>+ Frame</button>{frames.map((f,i)=><button key={f.id} className={i===current?'active':''} onClick={()=>onChange(i)}>#{i+1}<small>{f.duration_ms}ms</small></button>)}{frames[current]&&<button onClick={()=>onDuplicate(frames[current].id)}>Dupliquer</button>}</div>}
