import { useState } from 'react';
export function ElementForm({onSubmit}){const [v,setV]=useState({type:'object',image_ref:'',x:540,y:960,rotation:0,scale:1,layer_order:1});const set=(k,x)=>setV({...v,[k]:x});return <form className="panel" onSubmit={e=>{e.preventDefault();onSubmit(v)}}><h3>Ajouter un élément</h3><select value={v.type} onChange={e=>set('type',e.target.value)}><option>character</option><option>object</option><option>background</option><option>text</option></select><input placeholder="URL / image_ref" value={v.image_ref} onChange={e=>set('image_ref',e.target.value)}/>{['x','y','rotation','scale','layer_order'].map(k=><label key={k}>{k}<input type="number" step={k==='scale'?'0.1':'1'} value={v[k]} onChange={e=>set(k,Number(e.target.value))}/></label>)}<button>Ajouter</button></form>}
